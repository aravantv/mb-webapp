General reaction chain:
* New model
* -> Abstract model is computed
* -> rendering is done

* Model changes
* -> Abstract model is partly recomputed, but should be somehow done only incrementally!
* -> rendering is done

Other chain:
* User changes abstract model
* -> model changes

Clicking somewhere else should confirm

# do metamodel editor (directly, no generality, just to play with mobx)
# tree:
  . xxxx -+
  .. yyyy -+
  .. zzzz -+
  . wwww -+ 
  +
  MakeTree(Widget)
# projectional editing
# svg
# metamodel
# metamodel for MVC
# constraints on metamodel (increment 1: simple constraints only)
# css
# context menu? (somehow prefer without...)